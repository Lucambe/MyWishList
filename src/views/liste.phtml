<nav aria-label="breadcrumb">
  <ol class="breadcrumb">
    <li class="breadcrumb-item"><i class="fas fa-home"></i></li>
    <li class="breadcrumb-item active" aria-current="page"><?= $liste->titre ?></li>
  </ol>
</nav>
<div class="card text-center mt-2">
  <div class="card-body">
    <blockquote class="blockquote mb-0">
      <p><?= $liste->description ?></p>
      <footer class="blockquote-footer">Expire le <cite title="Source Title"><?= date("d/m/Y", strtotime($liste->expiration)) ?></cite></footer>
    </blockquote>
  </div>
</div>
<div class="row row-cols-1 row-cols-md-2">
  <?php foreach ($items as $item) : ?>
    <div class="col mb-4">
      <div class="card h-100 my-2">
        <img class="card-img-top" src="<?= $rootUri ?>/public/images/<?= $item->img ?>" alt="Image de l'objet">
        <div class="card-body">
          <h3 class="card-title"><?= $item->nom ?></h3>
          <p class="card-text"><span class="card-text badge badge-warning"><?= $item->tarif ?> €</span><?php if($reservations[$item->id]): ?> <span class="card-text badge badge-danger">Reservé</span> <?php else: ?> <span class="card-text badge badge-success">Disponible</span> <?php endif; ?></p>
          <p class="card-text"><?= $item->descr ?></p>
        </div>
        <div class="card-footer">
          <a class="btn btn-block btn-primary" href="<?= $router->pathFor('showItem', ['token' => $liste->token, 'id' => $item->id]) ?>" role="button">Plus d'infos</a>
        </div>
      </div>
    </div>
  <?php endforeach; ?>
</div>

<div class="row row-cols-1 row-cols-md-2">
    <?php foreach ($messages as $message) : ?>
        <span class="border border-primary">
            <p class="card-text"><?= $message->message ?></p>
        </span>
    <?php endforeach; ?>
</div>

<form id="lmsg" method="get">
    <fieldset>
        <legend>Envoyer un message</legend>
        <div class="input-group">
            <div class="input-group-prepend">
                <span class="input-group-text">Entrez votre prénom/pseudo ici</span>
            </div>
            <input type="text" name="prn" class="form-control" placeholder="Indiquez votre prénom ici">
        </div>
            <small id="emailHelp" class="form-text text-muted">Il ne sera pas visible par le créateur de la liste.</small>
        <div class="input-group">
            <div class="input-group-prepend">
                <span class="input-group-text">Écrivez votre message ici</span>
            </div>
            <input type="textarea" id="lmsg_txt" name="txt" class="form-control" aria-label="With textarea" placeholder="..." required>
        </div>
        <button class="btn btn-primary" type="submit" name="valider_message" value="valid_lmsg">Envoyer</button>
    </fieldset>
</form>

